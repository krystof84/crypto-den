import React from 'react';
import { SidebarContainer } from 'components/Currency/SideBar/SideBarStyles';

const SideBar = () => {
  return(
    <SidebarContainer>
      Sidebar
    </SidebarContainer>
  );
};

export default SideBar;